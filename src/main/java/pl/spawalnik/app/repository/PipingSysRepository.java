package pl.spawalnik.app.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import pl.spawalnik.app.model.PipingSys;

public interface PipingSysRepository extends JpaRepository<PipingSys, Long> {
}
